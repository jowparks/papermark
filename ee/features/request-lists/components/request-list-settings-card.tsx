// self-host shim: graceful-degradation replacement for the license-gated module. Replace with the real licensed module if available.

export function RequestListSettingsCard(_props: {
  dataroomId: string;
  teamId: string;
  requestListEnabled?: boolean;
}) {
  return null;
}
